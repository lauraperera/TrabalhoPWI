<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,300,300italic,400italic,600' rel='stylesheet' type='text/css'>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700" rel="stylesheet">
    <style>
        html, body{
            min-height: 100%;
            font-family: Roboto, Arial, sans-serif;
            font-size: 14px;
            color: rgb(0, 0, 0);
            margin: 150px;
        }    
        form{
           /* border-top: 1px solid #ff67f2;*/
        }
        input{
            width: 100%;
            padding: 5px;
            vertical-align: left;
            margin: 8px 0;
            display: inline-block;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
        button{
            width: 100%;
            padding: 10px 0;
            margin: 10px auto;
            border-radius: 5px; 
            border: none;
            background: #ff67f2; 
            font-size: 14px;
            font-weight: 600;
            color: #fff;
        }
        h1{
            margin: 0;
            font-weight: 400;
            text-align: center;
        }
        button:hover {
            opacity: 0.8;
        }
    
        .bloco-dados{
            padding: 16px 0;
            text-align:left;
        }
        .bloco-lembrar label{
            width: auto;
        }
        .bloco-lembrar input{
            width: auto;
        }
            
    </style>
</head>
<body>
    <form action="/login_realizado" method="POST" autocomplete="off">
        <h1>Login</h1>
        <div class="bloco-principal">
            <div class="bloco-dados">
                <label for="usuario">Nome de usuário</label>
                <input type="text" placeholder="Digite seu nome de usuário" name="usuario" required>
                <label for="email">Email</label>
                <input type="email" placeholder="Digite seu email" name="email" required>
                <label for="senha">Senha</label>
                <input type="password" placeholder="Digite sua senha" name="senha" required>
            </div>
            <button type="submit">Login</button>
            <div class="bloco-lembrar" style="background-color: white;">
                <label style="padding-left: 15px" name="lembrar">
                    <input type="checkbox"  checked="checked" name="lembrar"> Lembrar meus dados
                </label>
            </div>
        </div>
    </form>
</body>
</html>